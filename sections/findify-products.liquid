{%- assign page_url = content_for_header | split: '"pageurl":"' | last | split: '"' | first | split: request.host | last | replace: '\/','/' | replace:'%20',' ' | replace:'\u0026','&' | replace: '%7C', '|' | replace: '%27', "'" -%}
{%- assign findify_product_param = page_url | split: 'sections=findify-products&' | last -%}
{%- assign products = findify_product_param | split: 'products=' | last | split: '&' | first -%}

{% for product in products %}
    {% render 'findify-product-card', card_product: all_products[product], widget_type: 'search' %}
{% comment %}{% endcomment %}
{% endfor %}